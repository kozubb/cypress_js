{
  "name": "cypress_js",
  "version": "1.0.0",
  "description": "cypressproject",
  "main": "cypress.config.js",
  "scripts": {
    "cy:run": "npx cypress run",
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:run:chrome": "npx cypress run --browser chrome --headless",
    "merge-reports": "mochawesome-merge cypress/reports/*.json > cypress/reports/merged-report.json",
    "generate-html": "marge cypress/reports/merged-report.json --reportDir=cypress/reports"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^15.7.0",
    "cypress-junit-reporter": "^1.3.1",
    "cypress-multi-reporters": "^2.0.5",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.1.0"
  }
}
